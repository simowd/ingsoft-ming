<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.LanguageDao">
    <select id="LanguagesList" resultType="bo.ucb.edu.ingsoft.models.Language">
        SELECT
               id_language as idLanguage,
               language as language
        FROM languages
        WHERE
            status = 1
    </select>
</mapper>
