<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.PriceDao">
    <select id="findBySale" resultType="bo.ucb.edu.ingsoft.models.Price">
        SELECT
               id_price as idPrice,
               id_game as idGame,
               price as price,
               sale as sale
        FROM price
        WHERE
            sale > 0
    </select>
</mapper>
