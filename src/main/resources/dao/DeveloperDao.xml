<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.DeveloperDao">
    <insert id="createDeveloper" parameterType="bo.ucb.edu.ingsoft.models.Developer">
      INSERT INTO developers
        VALUES (
             null,
             #{idPublisher},
             #{developer}
        )
    </insert>

    <select id="findByIdDevoper" resultType="bo.ucb.edu.ingsoft.models.Developer">
        SELECT
               id_developer as idDeveloper,
               id_publisher as idPubliser
               developer as developer
        FROM directx
        WHERE
            id_directx = #{idDirectx}
    </select>

    <select id="findByIdPublisher" resultType="bo.ucb.edu.ingsoft.models.Developer">
        SELECT
               id_developer as idDeveloper
        FROM developers
        WHERE
            id_publisher = #{idPublisher}
    </select>

    <select id="getLastInsertId" resultType="java.lang.Integer">
        SELECT LAST_INSERT_ID()
    </select>
</mapper>
